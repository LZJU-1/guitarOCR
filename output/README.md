# Output

The default command-line interface writes reconstructed files and preview PDFs
under this directory when an explicit destination is not supplied. Generated
outputs are intentionally ignored by Git.
