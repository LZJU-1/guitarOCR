# Generated database

This directory is intentionally empty in Git. Dataset manifests, rendered pages,
labels, training crops, reports, temporary Java classes and training checkpoints
are generated here by the scripts under `scripts/`.

The five inference checkpoints required by a fresh clone are versioned separately
under `weights/`.
